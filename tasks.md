# Tasks instructions

Mark implemented tasks as you proceed. Make sure to update the TESTING.md document with the new features and their testing status (unit + e2e).

# Tasks list
 
[x] The tasks are on some screens not displayed anymore. At least the tasks side bar should show all global tasks, and the page with the overlay should show only tasks related to that page.
[x] task --edit should support --status flag/filter as well
[x] when tasks is returned in CLI, it should also return the full path to the task .md file
[x] Screenshots can't be taken anymore. They are not getting saved to the file system, they are also not shown in the UI.
[x] Fix: Unchecked runtime.lastError: Either the '<all_urls>' or 'activeTab' permission is required.
[x] Rename the whole project to "proto", package, cli, extension, accurances in UI etc...
[x] Additional to the test-id, add the CSS selector as well (in the tasks front matter)
[x] add console logs when task is modifed or added via API
[x] all tasks return same selector (same test-id) in my app